@model IEnumerable<QLVTFinal.Models.Category>

@{
    ViewBag.Title = "Quản lý danh mục cha";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Quản lý danh mục cha</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Danh mục cha
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nameCategory)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.idCategory }) |
                @Html.ActionLink("Chi tiết", "Details", new { id = item.idCategory }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.idCategory })
            </td>
        </tr>
    }

</table>

@Html.ActionLink("Mục đã xóa", "Recycle")
